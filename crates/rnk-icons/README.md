# rnk-icons

Terminal icon helpers for `rnk` applications.

This crate contains Nerd Font icon constants and lookup helpers for file,
development, Git, weather, UI, and miscellaneous terminal UI use cases.

Use it directly when an application needs icons without depending on the full
`rnk` UI framework.
